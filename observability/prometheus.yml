global:
  scrape_interval: 15s

storage:
  tsdb:
    retention:
      time: 35d

scrape_configs:
  - job_name: indication-scout
    metrics_path: /metrics/
    static_configs:
      - targets:
          - api:8000
